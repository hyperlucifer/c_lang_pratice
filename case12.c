// Online C compiler to run C program online
#include <stdio.h>
#define pi 3.14
int main() {
    int n,a,mult=0;
    printf("enter the number = ");
    scanf("%d%d", &n, &a);
    for (int i = n;i != 1;i--) {
        mult = mult + n;
        
    }
    printf("%d", mult);
 
}